{
  "name": "doctor-insurance-chatbot",
  "version": "4.0.0",
  "dependencies": {
    "googleapis": "^140.0.1"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/line-webhook.js && node --check api/line-agent.js && node --check api/premium-quote.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
